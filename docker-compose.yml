services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: imani
      POSTGRES_PASSWORD: imani
      POSTGRES_DB: imani_cms
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
